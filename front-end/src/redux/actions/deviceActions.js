// device actions
import {
    LOADING_DEVICES, 
    GET_DEVICES, 
    GET_DEVICE, 
    GET_LIKE_DEVICES, 
    GET_UNLIKE_DEVICES, 
    POST_DEVICE_COMMENT 
} from '../types';

// axios
import axios from 'axios';